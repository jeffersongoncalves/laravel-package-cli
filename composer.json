{
    "name": "jeffersongoncalves/laravel-package-cli",
    "description": "Scaffold new open-source Laravel packages with git already configured, built with Laravel Zero. Designed to be driven non-interactively (by an AI agent or a script) via arguments and flags.",
    "keywords": ["laravel", "cli", "laravel-zero", "laravel-package", "scaffold", "spatie", "devtools"],
    "homepage": "https://github.com/jeffersongoncalves/laravel-package-cli",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Jefferson Simão Gonçalves",
            "email": "gerson.simao.92@gmail.com"
        }
    ],
    "require": {
        "php": "^8.3",
        "jeffersongoncalves/laravel-zero-package-scaffold": "^1.0"
    },
    "require-dev": {
        "jeffersongoncalves/laravel-zero-self-update": "^1.1",
        "laravel-zero/framework": "^13.0",
        "laravel/pint": "^1.25",
        "mockery/mockery": "^1.6",
        "pestphp/pest": "^3.8|^4.7",
        "phpstan/phpstan": "^2.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "build": "php laravel-package app:build laravel-package --build-version=\"$(git describe --tags --abbrev=0 2>/dev/null || echo unreleased)\"",
        "lint": "./vendor/bin/pint",
        "test:lint": "./vendor/bin/pint --test",
        "test:unit": "./vendor/bin/pest --parallel",
        "test:types": "./vendor/bin/phpstan analyse --memory-limit=256M",
        "phpstan": "./vendor/bin/phpstan analyse --memory-limit=256M",
        "phpstan-baseline": "./vendor/bin/phpstan analyse --generate-baseline --memory-limit=256M",
        "test": [
            "@test:unit",
            "@test:lint"
        ]
    },
    "config": {
        "preferred-install": "dist",
        "sort-packages": true,
        "optimize-autoloader": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "bin": ["builds/laravel-package"]
}
